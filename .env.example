# DR DXB Server — Environment configuration
# For local dev: copy to .env.local
# For Vercel: add in Project → Settings → Environment Variables (optional)
# All deployment and signing happens in the browser; these only affect RPC/explorer URLs.

# Ethereum mainnet RPC (optional; fallback: https://eth.llamarpc.com)
NEXT_PUBLIC_ETH_RPC_URL=https://eth.llamarpc.com

# BNB Smart Chain RPC (optional; fallback: https://bsc-dataseed1.binance.org)
NEXT_PUBLIC_BSC_RPC_URL=https://bsc-dataseed1.binance.org

# Etherscan API key (optional; for tx status / contract info from Etherscan API)
# Get a free key at https://etherscan.io/myapikey
NEXT_PUBLIC_ETHERSCAN_API_KEY=
